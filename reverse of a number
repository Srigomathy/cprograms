#include <stdio.h>

void main()
{
   int n,rem,min=0;
   printf("enter a number:");
   scanf("%d",&n);
   while(n!=0)
   {
       rem=n%10;
       n=n/10;
       if(rem>0)
       {
          printf("%d",rem);
       }
   }
}
