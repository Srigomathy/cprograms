#include <stdio.h>

void main()
{
   int n,i;
   float fact=1,sum=0;
   printf("enter n:");
   scanf("%d",&n);
   for(i=1;i<=n;i++)
   {
       fact=fact*i;
       sum=(i/fact)+sum;
   }
   printf("sum of series is: %f",sum);
}
